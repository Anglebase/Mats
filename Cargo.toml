[package]
name = "mats"
version = "0.1.5"
edition = "2021"
description = "A Rust library for handling matrices and vectors."
repository = "https://github.com/Anglebase/Mats.git"
license = "MIT"

[dependencies]
gl = { version = "0.14.0", optional = true }

[features]
glsl = ["gl"]